#include <stdio.h>
/**
 * main - Entry point
 *
 * Function: Print alphabet in lowercase
 *
 * Return: Always (0) (Success)
 */
int main(void)
{
	int i;

	for (i = 'a'; i <= 'z'; i++)
		putchar(i);
		putchar('');
	return (0);
}
